# 交接说明（给新工作区的会话看）

项目在 **`D:\Acm-Tracker`**（一个本地 git 仓库，main 分支，已推送 GitHub）。
看 `AGENTS.md` 了解项目本身；这份文档说的是「现在到哪一步了、接下来做什么、哪些坑已经填过」。

## 一句话现状

**v0.1.8 已经发布**（标签 `v0.1.8`，GitHub Actions 自动打包并建了 Release，附件是安装包）。
`main` 分支干净、和远端同步；桌面上那份 `C:\Users\summer\Desktop\ACM Trainer` 也是 0.1.8。
每一版的更新说明在 `outputs\release-notes-vX.Y.Z.md`，看那个比看这份更准。

## 最近几版做了什么

0.1.5 到 0.1.8 之间：左侧菜单分页 + 今日卡片（手感 / 热身 / 复盘）、成长页网格、
数据备份与导出导入、训练计划可混进 AtCoder 和洛谷的题（三平台配额，每天每平台最多一道）、
比赛日历接三家平台、洛谷赛程抓取补上 cookie、洛谷题库按难度档抓。

## 待办（按优先级）

1. **让用户完整跑一次模型训练**（设置 → 推题模型 → 开始训练，默认 300 场、十几分钟）。
   之前只在小样本上验过链路，没赢过「只看难度差」的基线，所以模型一直是关闭状态。
2. 用户提过但没做的：**语音输入**（当时没说清要什么）、**自动检查更新**、**Mac 版**。
3. 宣传：B 站宣传版和 v0.1.3 更新版发过；v0.1.7 更新视频 + 封面 + 简介都做好了
   （`.dev\make-video-v017.mjs`、`outputs\v0.1.7-视频简介.md`），发不发看用户。
   群号 1124017564 已经在视频、简介、README 里。

## 关键决策（别推翻，除非有数据）

- **不做「做了这道题能涨多少分」的预测**。练什么题不是随机分配的，那样学出来是「谁强」。
  改成预测「能不能做出」，这是良定义、可验证的二分类问题。
- **模型门槛不放松**：AUC ≥ 0.75 且比基线高 0.005（或对数损失低 1%）。宁可不用。
- **不用深度学习**，逻辑回归够了；权重能直接看，出问题好查。
- **零依赖**是卖点，别为了新功能引入 npm 运行时依赖。

## 用户的工作方式

- 中文交流，非技术性描述；他会亲自测试并给很准的反馈（视觉细节尤其敏感）。
- 反感「正确的废话」和 AI 味表达；汇报先给结论和数据。
- 他的 PowerShell 禁止 `.ps1`，一律 `npm.cmd`。
- **运行打包版要注意**：从 Codex 自己的终端里启动 `ACM Trainer.exe`，`%APPDATA%` 会被重定向到
  沙箱容器里另一个几乎是空的数据目录（表现成「题库 0 道」，看着像数据丢了）。验证打包版请用
  资源管理器启动，或者直接问程序内置服务的 `/api/health`（里面报的版本、题库条数最可信）。

## 有件事要提醒

这个项目曾经被误删过一次（2026-09-22 晚上，`outputs\acm-trainer` 整个消失，
回收站是空的）。当时靠 GitHub 上的代码 + 我留的测试数据副本恢复的。
**别轻易给出 `Remove-Item -Recurse -Force` 这类命令让用户执行**，
要删就先确认新位置有完整副本。
