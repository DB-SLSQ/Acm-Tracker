# 交接说明（给新工作区的会话看）

项目在 **`D:\Acm-Tracker`**（一个本地 git 仓库，main 分支，已推送 GitHub）。
看 `AGENTS.md` 了解项目本身；这份文档说的是「现在到哪一步了、接下来做什么、哪些坑已经填过」。

## 一句话现状

**v0.1.4 的代码已经写完、打包好、推送到 GitHub 了**（提交 `69a7cf5`，标签 `v0.1.4`）。
安装包在 `D:\Acm-Tracker\dist\ACM Trainer Setup 0.1.4.exe`（108 MB）。

## 最近这一版做了什么

1. **题单标签全部显示中文**（37 个 Codeforces 官方 tag），鼠标悬停能看到英文原文。
   映射表在 `public/app.js` 顶部的 `TAG_ZH`。
2. **单个标签的占比上限可调**，默认 40%，位置：设置 → 题单构成。
   实测 99 题的题单：40% 时出现最多的标签各占 40 题，25% 时每个标签最多 25 题。
3. **推题模型**：从官方比赛数据学「以你现在的水平能不能做出这道题」，
   设置 → 推题模型里点「开始训练」，或跑 `npm.cmd run train`。
   会严格按比赛时间切分留出集，跟「只看难度差」的基线比，**赢不过就不用**。

## 待办（按优先级）

0. **有一批新改动还没进仓库**：按知识方向的配额制分配题量 + 训练计划里的「隐藏标签」开关。
   代码改在 C 盘会话目录的 `work\planwork\`（已经用真实数据验证过），
   用同目录下的 `应用到项目.cmd` 双击覆盖到项目里，然后重新 `npm.cmd run build` 打包。
   如果 v0.1.4 的 Release 已经发过，记得先把 `package.json` 的版本改成 0.1.5 再打包。
1. **确认 v0.1.4 的 GitHub Release 建了没有**。如果没建：标签选 `v0.1.4`，标题 `0.1.4`，
   附件传 `dist\ACM Trainer Setup 0.1.4.exe`，说明用 `outputs\release-notes-v0.1.4.md`
   （那份文件在旧会话目录 `C:\Users\summer\Documents\Codex\2026-09-20\new-chat\outputs\` 里）。
2. **让用户完整跑一次模型训练**（默认 300 场，十几分钟），看看能不能赢过基线。
   我这边只能用 5 场比赛验证过链路：5704 条样本，模型 AUC 0.968、基线 0.968，**没赢，所以没启用**。
   这个结果是符合预期的（样本太少），但需要真实的大样本跑一次才知道值不值得留着。
3. 用户之前提过但没做完的：**语音输入**（他没说完就被打断了，需要问清楚）；
   **自动检查更新**；**Mac 版**。
4. 宣传：B 站视频已经发过（宣传版 4 分半 + v0.1.3 更新版 1 分 22 秒），
   新版本发布后记得让用户更新简介里的下载链接。群号 1124017564 要做进视频/文档/README。

## 关键决策（别推翻，除非有数据）

- **不做「做了这道题能涨多少分」的预测**。练什么题不是随机分配的，那样学出来是「谁强」。
  改成预测「能不能做出」，这是良定义、可验证的二分类问题。
- **模型门槛不放松**：AUC ≥ 0.75 且比基线高 0.005（或对数损失低 1%）。宁可不用。
- **不用深度学习**，逻辑回归够了；权重能直接看，出问题好查。
- **零依赖**是卖点，别为了新功能引入 npm 运行时依赖。

## 用户的工作方式

- 中文交流，非技术性描述；他会亲自测试并给很准的反馈（视觉细节尤其敏感）。
- 反感「正确的废话」和 AI 味表达；汇报先给结论和数据。
- 他的 PowerShell 禁止 `.ps1`，一律 `npm.cmd`。
- **沙箱限制**：需要联网的操作（拉 Codeforces 数据、TTS 配音）要申请网络权限；
  写工作区以外的路径（比如 D 盘）也要申请。

## 有件事要提醒

这个项目曾经被误删过一次（2026-09-22 晚上，`outputs\acm-trainer` 整个消失，
回收站是空的）。当时靠 GitHub 上的代码 + 我留的测试数据副本恢复的。
**别轻易给出 `Remove-Item -Recurse -Force` 这类命令让用户执行**，
要删就先确认新位置有完整副本。
